export interface NoteProps {
    id: number,
    title: string,
    content: string,
    creatAt: string,
    updatedAt: string,
    tag: string,
    message: string,
    error: string,
    total_pages:number
}